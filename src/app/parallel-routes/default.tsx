import React from "react";

const page = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        boxShadow: "1px 2px 20px gray",
        borderRadius: "10px",
        height: "100%",
        width: "100%",
        textAlign: "center",
        padding: "10px",
      }}
    >
      Parallel Default route page
    </div>
  );
};

export default page;
